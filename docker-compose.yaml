version: "3"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    networks:
      - vstk-network

  backend:
    build:
      context: ./backent
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    networks:
      - vstk-network

networks:
  vstk-network:
    driver: bridge
